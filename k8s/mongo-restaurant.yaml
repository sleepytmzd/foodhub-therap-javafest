apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongodb-restaurant-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 200Mi

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongodb-restaurant-deployment
  labels:
    app: mongodb-restaurant
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mongodb-restaurant
  template:
    metadata:
      labels:
        app: mongodb-restaurant
    spec:
      containers:
      - name: mongodb-restaurant
        image: mongo:7.0.5
        ports:
        - containerPort: 27017
        env:
        - name: MONGO_INITDB_ROOT_USERNAME
          valueFrom:
            secretKeyRef:
              name: app-secret 
              key: mongo-user 
        - name: MONGO_INITDB_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: app-secret 
              key: mongo-password
        - name: MONGO_INITDB_DATABASE
          value: rest-service
        resources:
          requests:
            cpu: "100m"     
            memory: "250Mi"  
          limits:
            cpu: "500m"    
            memory: "1Gi"
        volumeMounts:
          - name: mongodb-data
            mountPath: /data/db
      volumes:
        - name: mongodb-data
          persistentVolumeClaim:
            claimName: mongodb-restaurant-pvc

---

apiVersion: v1
kind: Service
metadata:
  name: mongodb-restaurant-service
spec:
  selector:
    app: mongodb-restaurant
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017

